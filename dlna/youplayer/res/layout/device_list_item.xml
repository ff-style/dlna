<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/servce_list"
    android:gravity="center_vertical"

    >
    <ImageView 
        android:id="@+id/ctrl_list_item_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/tux_icon"
        android:layout_margin="5.0dip"
        />
    <TextView android:id="@+id/ctrl_list_item_name"
        android:layout_width="fill_parent"
        android:layout_height="20dp"
        android:textSize="14sp"
        android:textColor="@color/black"
        android:singleLine="true"
        android:layout_marginLeft="4dp"
        android:gravity="center_vertical"
        android:layout_centerVertical="true"
        android:text="@null" />
    <TextView android:id="@+id/ctrl_list_item_location"
        android:layout_width="fill_parent"
        android:layout_height="20dp"
        android:textSize="14sp"
        android:textColor="#000"
        android:singleLine="true"
        android:layout_marginLeft="4dp"
        android:gravity="center_vertical"
        android:layout_centerVertical="true"
        android:visibility="gone"
        android:text="@null" />
    <TextView android:id="@+id/ctrl_list_item_uuid"
        android:layout_width="fill_parent"
        android:layout_height="20dp"
        android:textSize="14sp"
        android:textColor="#000"
        android:singleLine="true"
        android:layout_marginLeft="4dp"
        android:gravity="center_vertical"
        android:layout_centerVertical="true"
        android:visibility="gone"
        android:text="@null"/>
    <TextView android:id="@+id/ctrl_list_item_type"
        android:layout_width="fill_parent"
        android:layout_height="20dp"
        android:textSize="14sp"
        android:textColor="#000"
        android:singleLine="true"
        android:layout_marginLeft="4dp"
        android:gravity="center_vertical"
        android:layout_centerVertical="true"
        android:visibility="gone"
        android:text="@null"/>
 
</LinearLayout>